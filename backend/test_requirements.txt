# Testing dependencies - Lightweight for CI/CD
pytest>=7.4.0
pytest-mock>=3.11.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Azure Functions testing
azure-functions>=1.23.0

# Additional testing utilities
requests>=2.28.0

# Note: Heavy ML dependencies (transformers, torch) removed for CI/CD speed
# Add back locally if needed for full ML testing