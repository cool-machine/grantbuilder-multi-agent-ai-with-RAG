# Ultra-minimal requirements for GitHub Actions deployment
# Only the absolute essentials to avoid grpcio compilation issues

azure-functions==1.18.0
requests==2.31.0

# Note: All other dependencies removed for CI/CD compatibility
# Full requirements available locally for development