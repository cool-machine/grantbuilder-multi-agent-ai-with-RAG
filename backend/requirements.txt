# Minimal Azure Functions requirements for deployment
azure-functions>=1.18.0
azure-functions-worker>=1.0.0

# Core API dependencies
requests>=2.31.0
PyPDF2>=3.0.1

# Environment management
python-dotenv>=1.0.0

# Note: Heavy dependencies (openai, reportlab, pytest) removed for CI/CD
# Add back locally if needed for local development