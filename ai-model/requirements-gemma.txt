# Optimized requirements for Gemma 3 270M deployment
# Carefully selected versions for stability and performance

# Core ML dependencies (updated for latest transformers compatibility)
torch>=2.0.1
git+https://github.com/huggingface/transformers@main  # Latest dev version for Gemma 3 support
tokenizers>=0.19.0
accelerate==0.23.0

# Flask and web server
Flask==3.0.0
Werkzeug==3.1.3
gunicorn==21.2.0

# Essential utilities
requests>=2.31.0
numpy>=1.24.3
psutil>=5.9.5

# HuggingFace hub (updated for Gemma 3 compatibility)
huggingface-hub>=0.34.0

# Security and optimization (updated for latest transformers)
safetensors>=0.4.3